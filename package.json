{
  "name": "keep-it-up",
  "version": "1.0.0",
  "description": "Keep the hosted website up & running. Don't let the free tier deployments sleep due to inactivity.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Rishabh Singh",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "puppeteer": "^22.12.1",
    "typescript": "^5.5.3"
  },
  "devDependencies": {
    "@types/puppeteer": "^7.0.4"
  }
}
